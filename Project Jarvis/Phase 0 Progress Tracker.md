---
title: Phase 0 Progress Tracker
type: tracker
source: "n/a — living note, tracks execution of [[Implementation Roadmap]] Phase 0"
---

# Phase 0 Progress Tracker

⬅ Back to [[Project Jarvis - Agentic OS]]

> Tracks real-world execution status against the infra items in [[Implementation Roadmap]] Phase 0, now running on the compressed timeline in [[Accelerated Launch Plan (2-Week Sprint)]] (go-live target 2026-07-15). Update the checkboxes and dates as items move — this note is the "reality" layer on top of the plan. For the two current blockers as single ownable next-actions, see [[Action Items]].

## Checklist

- [ ] **Submit Google Ads developer-token application** — longest external dependency on the critical path. [[Google Ads API]] / [[Recommendations]] #3 flagged applying "week 1"; [[Caveats]] notes an early-2026 review backlog. *Status as of 2026-07-01: confirmed NOT YET SUBMITTED.* A token now exists in `.env.local`, but a 2026-07-24 API Center screenshot confirms it's at **Explorer Access only** (Google's current name for the old Test-Access tier) — not Basic or Standard. **Still open**: needs an upgrade request to Basic, then Standard, before production API access is unblocked. (An earlier note today briefly and incorrectly marked this resolved at "Standard Access" based on an unverified report — corrected once the screenshot came in.) See [[Google Ads API]] "Developer token access level" section.
- [ ] **Provision the VPS** (Hetzner/DigitalOcean) — see [[Layer 1 - Host Infrastructure]]. **ON HOLD as of 2026-07-07** — pending budget/cost approval. Not a provider-choice or sequencing question; provisioning simply doesn't start until approval lands.
- [ ] **Harden the VPS** — non-root user, firewall, Tailscale, disk encryption. *Blocked behind the VPS hold above.*
- [ ] **Install the Claude Agent SDK runtime** — see [[Layer 2 - Agentic Harness]]. *Blocked behind the VPS hold above.*
- [x] **Stand up a private git repo for Skills/knowledge files** — decided **separate** from this vault's repo (`kitdea/project-jarvis`), since Skills/knowledge files are runtime agent assets, not wiki/planning content. Created locally at `~/Documents/project-jarvis-skills` (`skills/`, `knowledge/` dirs) with an initial commit; no GitHub remote yet by choice — push later once ready. See [[Layer 6 - Skills and Knowledge Injection]].
- [ ] **Create a secrets vault** (env vars or managed secrets store — never hardcode credentials). *Interim stopgap, updated 2026-07-07: the CallRail API key now lives in a git-ignored `.env` in the `project-jarvis-skills` repo (moved there from this vault, since that's where the runtime/test code that uses it actually lives) — never committed, single copy. This is dev-machine-only, not the production secrets store this checklist item is ultimately tracking; that still depends on the VPS.*
- [ ] **Get a working AccuLynx API key** — the issued key smoke-tested as `401 API Key is invalid or deactivated` on 2026-07-10 via the new [[AccuLynx]] MCP tool layer. GHL and CallRail's equivalent keys both passed the same test; this is isolated to AccuLynx. Blocks any real use of the AccuLynx connector until a fresh/reactivated key is obtained.
- [x] **Add rate limiting and auth to `server.py`'s `/chat`, `/remember`, `/speak`** — done 2026-08-13. Auth: a shared-secret token (`JARVIS_TOKEN` in a git-ignored `.env` at the vault root, loaded by the new `AuthGate` class) required on the `X-Jarvis-Token` header for all three routes; `server.py`'s own `/` handler stamps the token into `index.html` as `window.JARVIS_TOKEN` so the page can send it back, and `viewer/index.html`'s three `fetch` calls now send it via an `authHeaders()` helper. Rate limiting: a fixed-window `RateLimiter` (30 requests / 60s per client IP, shared across the three routes) — a plain fixed window rather than a sliding log, since this is guarding a single-user local tool against a runaway script/tab, not an adversary optimizing around window edges. Both denials (401/429) are recorded through the existing `AuditLog`. No token configured -> gate stays open with a startup warning, matching `PiperTTS`'s degrade-don't-raise pattern rather than locking the operator out. Still binds to `127.0.0.1` only, so this is insurance ahead of the VPS move, not a response to an active gap — see [[Security and Guardrails]]'s "audit everything" section, now fully closed out.
- [x] **Automate the daily ingestion/lint check** — was ad hoc, relying on someone remembering to say "lint the wiki." Added `scripts/daily-wiki-check.sh` + a real crontab entry (6:17am local daily). See log below, 2026-08-13.

## Re-verify (per [[Caveats]])
Pricing/rate-limit figures were "current as of mid-2026" — that window is now. Before budgeting, re-check:
- [ ] CallRail tier pricing ([[CallRail v3]])
- [ ] GoHighLevel rate limits ([[GoHighLevel v2]])
- [ ] AccuLynx rate limits + Partner Program status ([[AccuLynx]])

## Log
| Date | Update |
|---|---|
| 2026-07-01 | Note created. No Phase 0 items confirmed started yet — flagged by vault analysis. |
| 2026-07-01 | Go-live target set: 2026-07-15 (2-week sprint). Google Ads token confirmed not yet submitted — descoped from day-1 launch per [[Accelerated Launch Plan (2-Week Sprint)]]; submit today to start the fast-follow clock. |
| 2026-07-03 | Connected Google Ads to this Claude session via Supermetrics MCP (Shumaker Roofing account, ID `8531416360`) — live ad hoc reporting now possible. See [[Google Ads API]] "Interim option" section. Developer-token application status unchanged — still not submitted. |
| 2026-07-05 | Decided Skills/knowledge repo is separate from this vault. Created locally at `~/Documents/project-jarvis-skills`, no remote yet. Remaining Phase 0 items (Google Ads token, VPS provisioning/hardening, Agent SDK install, secrets vault, rate-limit re-verification) are still open — they require real-world accounts/access an AI session can't complete unattended. |
| 2026-07-07 | VPS provisioning put **on hold pending budget/cost approval**. This lands on the Week 1 checkpoint day in [[Accelerated Launch Plan (2-Week Sprint)]], which expected the VPS hardened by today — the Infra workstream hasn't started at all and is now behind schedule, which also blocks the downstream Agent SDK install and secrets-vault items. Go-live target (2026-07-15) is at risk if approval doesn't land soon. |
| 2026-07-07 | Received the CallRail API key. Added `.gitignore` to this vault (it had none) and initially stored the key in a git-ignored `.env` at the vault root — never committed, not pushed to `origin`. |
| 2026-07-07 | Per the "test locally before committing to VPS" decision: moved the CallRail key to `project-jarvis-skills/.env` (git-ignored there too) and added `scripts/test-callrail-auth.sh`, a smoke test hitting `GET /v3/a.json`. **Result: auth succeeded** — confirmed live against the Shumaker Roofing CallRail account (`numeric_id 241552663`). See [[CallRail v3]] "Live account" section. Script is uncommitted in that repo pending your review. |
| 2026-07-10 | Received the Google Ads OAuth **Client ID** — stored in `project-jarvis-skills/.env` as `GOOGLE_ADS_CLIENT_ID`, never in this vault. See [[Google Ads API]] "OAuth Client ID received" section. Note this is separate from the **developer-token application** above, which is still not submitted — the client ID alone doesn't unblock production API access. |
| 2026-07-10 | Built local MCP tool layers for two connectors: `mcp-servers/acculynx-mcp-server` (9 read-only tools) and `mcp-servers/ghl-mcp-server` (6 read-only tools), both stdio Node/TypeScript servers under `mcp-servers/` in this vault's own repo (not `project-jarvis-skills`). See [[AccuLynx]] and [[GoHighLevel v2]] "MCP tool layer" sections. Neither is wired into Claude Code yet (`claude mcp add` not run) and neither is committed — currently untracked working-tree files. |
| 2026-07-10 | Added a third MCP tool layer, `mcp-servers/callrail-mcp-server` (6 read-only tools). Same pattern/repo location as the two above; also not wired into Claude Code and not committed. See [[CallRail v3]] "MCP tool layer" section. |
| 2026-07-10 | Smoke-tested all three newly-wired MCP tool layers (GHL, CallRail, AccuLynx) with a live read call each. **GHL and CallRail passed** (real pipeline/account data returned). **AccuLynx failed**: `list_users` → `401 API Key is invalid or deactivated`. Server is wired and reachable — this is a bad/expired key, not a config problem. See [[AccuLynx]] "Credential status." Need a fresh AccuLynx API key before that connector is usable. |
| 2026-07-22 | **Operator instruction: stop flagging the VPS budget approval item.** No progress since the 2026-07-07 hold; the 2026-07-15 go-live target has now passed with the item still unresolved (surfaced across three consecutive lint passes — see [[Ingestion Log]]). Operator has explicitly deferred this: they will report back once budget approval lands. Per that instruction, this item and the go-live-date staleness it causes across [[GOALS]], [[Action Items]], and [[Accelerated Launch Plan (2-Week Sprint)]] should be treated as **intentionally parked, not silently stale** — don't re-chase or re-surface as a fresh finding until the operator updates it. |
| 2026-07-24 | Smoke-tested the live marketing MCP tool layers: CallRail (`list_accounts`/`list_companies`) and GHL (`list_contacts`/`list_opportunities`/`get_pipeline_stages`) both passed with real Shumaker Roofing data; Supermetrics and Semrush connectors also confirmed live (Semrush `domain_rank` returned real SEO data for shumakerroofing.com). Found a `GOOGLE_ADS_DEVELOPER_TOKEN` value already present in `.env.local`, initially flagged as unconfirmed tier. |
| 2026-07-24 | Operator initially reported the developer token as "confirmed" at Standard Access; checklist item was briefly marked done on that basis. **Correction, same day:** a screenshot of the actual Google Ads API Center shows **Access level: Explorer Access**, not Standard. Reverted the checklist item to open. The single longest external dependency on the Phase 0 critical path is **still unresolved** — needs a Basic Access upgrade request (then Standard) before production quota is available. See [[Google Ads API]] "Developer token access level" section for the screenshot-sourced detail. |
| 2026-07-24 (logged retroactively 2026-08-05) | Built `voice-app` ("Jarvis Lite") — a local Electron desktop prototype of the Layer 5 voice bolt-on, scoped to Marketing/Strategy/Info only (GHL + CallRail tools; AccuLynx and reporting/infra deliberately excluded). Speech-in via the browser's Web Speech API, speech-out via OS `speechSynthesis` — no external STT/TTS credential needed. This had been sitting undocumented in the working tree for 12 days, found only via `git status` during a later session. See [[Layer 5 - Communication Channels]] "Local prototype" section. |
| 2026-08-05 | Fixed a real bug in `voice-app/main.js`: the Agent SDK's default `permissionMode` prompts for interactive tool-use approval, but the renderer has no UI to answer it — any real GHL/CallRail question would have silently stalled. Set `permissionMode: 'bypassPermissions'` (+ required `allowDangerouslySkipPermissions: true`); safe because read-only enforcement already lives at the connector level. Verified live post-fix: correctly returned 10 GHL pipelines and a 102-call CallRail count instead of an authorization error. Committed to git (previously the fix would have sat uncommitted like everything else here). |
| 2026-08-05 | Cleared two long-standing housekeeping gaps: (1) confirmed `mcp-servers/` (all three servers) is now committed to git — it was committed at some point between sessions, no longer matches the 2026-07-10 "untracked working-tree files" note above. (2) Ran `claude mcp add` for `ghl`, `callrail`, and `acculynx`, sourcing each server's own `.env` via `-e` flags — all three now show `√ Connected` in `claude mcp list` as permanently registered project-scoped servers, not just ad-hoc session tools. AccuLynx registers and connects fine at the transport level; its `401` credential problem (see below) is unrelated to wiring and remains open. |
| 2026-08-13 | **Operator decision: `voice-app` retired, not evolved.** Operator confirmed a working Jarvis already exists elsewhere, so the local Electron prototype is no longer needed — it already served its purpose (de-risked the Layer 5 architecture, surfaced/fixed the `permissionMode` bug) before the VPS existed. No further work planned on it; left in place as reference only. See [[Layer 5 - Communication Channels]] "Local prototype" section, now marked retired. |
| 2026-08-13 | Security review of `server.py` against [[Security and Guardrails]]'s "audit everything" rule found the live server only printed one-line summaries to stdout — no persistent log, no rate limiting or auth on `/chat`/`/remember`/`/speak`. Fixed the logging half: added `AuditLog`, appending one JSON line per request (timestamp, route, session id, client IP, params, outcome) to `audit.log.jsonl` (git-ignored), wired into all three routes. Rate limiting/auth remain open — added as a new Phase 0 checklist item above, since they're currently masked by the `127.0.0.1`-only bind but block a safe VPS move. |
| 2026-08-13 | **Operator decision: `jarvis-dashboard` also retired, not evolved.** `jarvis-dashboard/` (browser-based Agent SDK app, GHL+CallRail scoped, self-documented as `voice-app`'s successor) had gone untouched since before `voice-app`'s retirement above and was never named in that decision. Operator confirmed it is also retired — `server.py` + `viewer/` (this repo's vault chat/capture/voice server, under active development this session) is the working Jarvis, not `jarvis-dashboard`. No further work planned on `jarvis-dashboard`; left in place as reference only. See [[Layer 5 - Communication Channels]] "Local prototype #2" section. |
| 2026-08-13 | Formalized the ingestion/lint check as a real OS-level cron job rather than an ad hoc habit — noted this vault already leans on a "scheduled daily un-ingested-clippings check" per [[Ingestion Log]], but nothing was actually automated. Added `scripts/daily-wiki-check.sh`, which runs Claude Code headlessly (`claude -p ...`) against this vault to ingest anything new in `Clippings/` and run the Lint workflow, logging output to `scripts/logs/` (git-ignored). Installed via `crontab -e`, daily at 6:17am local. Deliberately **not** built on the in-session `CronCreate` tool or `/loop` — both are tied to a live Claude Code session and don't survive it ending; a real crontab entry does. Findings still land in this log and get flagged for operator sign-off per CLAUDE.md's Lint workflow — this does not auto-fix contradictions. |
| 2026-08-13 | Closed out the last open item from [[Security and Guardrails]]'s "audit everything" section: added token auth (`AuthGate`, shared secret via `JARVIS_TOKEN` in a git-ignored `.env`) and per-IP rate limiting (`RateLimiter`, 30 req/60s fixed window) to `/chat`, `/remember`, `/speak` in `server.py`, plus a matching `authHeaders()` change in `viewer/index.html` so the page's own requests carry the token. Denials audit-log the same as successes. Smoke-tested locally: unauthenticated requests get `401`, the 31st request in a window gets `429`, a correctly-authed request still returns real Piper audio. The vault's still-running production `server.py` process (started before this change) needs a restart to pick this up — not restarted automatically since it's a live process serving the operator. |

---
⬅ Back to [[Project Jarvis - Agentic OS]]
