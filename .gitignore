.env
.env.*
.claudian/sessions/

# Dependencies — reinstallable from lockfiles, and the bundled Claude Agent SDK
# and Electron binaries are ~270MB each, which no git remote will accept.
node_modules/

__pycache__/
*.pyc

# server.py's audit trail — user questions/params, not vault content.
audit.log.jsonl

# scheduled wiki-check run logs
scripts/logs/
